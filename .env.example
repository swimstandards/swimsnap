# SwimSnap Environment Configuration Example

# Base URL for your local or production instance
BASE_URL=http://localhost/swimsnap

# MongoDB Configuration
# If MONGODB_URI is not set, the app will default to using the meta/ folder for storing metadata.
# To enable MongoDB, uncomment and configure the following lines:
# MONGODB_URI=mongodb://localhost:27017
# MONGODB_URI=mongodb://yourUsername:yourPassword@your.mongo.host:27017/?authSource=admin
# DBNAME=swimsnap

MONGODB_URI=
DBNAME=

# reCAPTCHA keys (used for uploads to prevent spam)
RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# Application environment: DEV or PROD
APP_ENV=DEV

# Google Analytics Measurement ID (used in layout.php if set)
GTAG_ID=